import React from 'react';

const PreventSettings = () => {
  return (
    <div className="preventSettings">
      settings are disabled for the demo user. however, you're free to edit the profile.
    </div>
  );
};

export default PreventSettings;
